<h1>New order</h1>

Current user - <%= self.current_user.id %> - </br>
Company abbr - <%= Company.find(self.current_user.company_id).abbr %> - </br>
Jobcount - <%= Company.find(self.current_user.company_id).jobcount %> - </br>

<%= render 'form' %>

<%= link_to 'Back', orders_path %>
